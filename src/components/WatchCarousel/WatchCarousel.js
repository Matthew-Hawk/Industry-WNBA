import './WatchCarousel.scss'

const WatchCarousel = ({ carouselTitle }) => {
    return (
        <div className="watch-carousel">
            <h2>{carouselTitle}</h2>
        </div>
    )
}

export default WatchCarousel;